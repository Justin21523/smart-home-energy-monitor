{
  "name": "smart-home-energy-monitor",
  "version": "1.0.0",
  "description": "Smart home energy dashboard with live reload using live-server",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --host=0.0.0.0 --port=3000 --watch=.",
    "demo:dev": "npm --prefix demo-app run dev",
    "demo:build": "npm --prefix demo-app run build",
    "demo:preview": "npm --prefix demo-app run preview",
    "build": "npm --prefix demo-app run build"
  },
  "devDependencies": {
    "live-server": "^1.2.0",
    "three": "^0.178.0"
  },
  "author": "",
  "license": "ISC"
}
